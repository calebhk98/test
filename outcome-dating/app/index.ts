import { registerRootComponent } from 'expo';
import App from './App';

// registerRootComponent calls AppRegistry.registerComponent('main', () => App),
// and sets up the environment appropriately for Expo, whether the app is
// running with Expo Go, a development build, or a native production build.
registerRootComponent(App);

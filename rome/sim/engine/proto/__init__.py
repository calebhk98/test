"""engine.proto: protocol.py split by subject. See rome/sim/ARCHITECTURE.md.

protocol.py itself is now a thin re-export shim over this package, kept so
`from engine.protocol import X` goes on working for every X it exported
before the split.
"""
